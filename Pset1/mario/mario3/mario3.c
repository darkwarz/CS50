#include <cs50.h>
#include <stdio.h>

int main(void)
{
    do
    {
        int n = get_int("Positive number: ");
    }
    while (n < 0);

    for (int i = 0; i < n; i++)

    int n = get_int("Number: ");

    {
        printf("#\n");
    }
}